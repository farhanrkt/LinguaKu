# Deploy separately from the app. Nothing in src/ depends on this existing.
name = "linguaku-sync"
main = "index.ts"
compatibility_date = "2026-08-11"

[[d1_databases]]
binding = "DB"
database_name = "linguaku"
# Filled in by `wrangler d1 create linguaku`.
database_id = ""
